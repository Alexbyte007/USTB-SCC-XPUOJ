---
sectionTitle: "数据范围与提示"
type: "Text"
---
数据范围与约束：

| 项目 | 约束 |
| --- | --- |
| `x`、`w_gate`、`w_up`、`y` | `bf16` |
| `b_gate`、`b_up` | `fp32` |
| tensor 存储 | 全部 contiguous，row-major |
| 可修改参数 | 只能写 `y`，不允许修改其他输入 tensor |
| 输出要求 | `y` 必须完整覆盖，不能依赖初值 |
| 边界要求 | `M`、`K`、`N` 不保证是 block size 或 tile size 的倍数 |
| 正确性容差 | `rtol = 5e-2`，`atol = 5e-2` |

真实测试点如下：

| 测试点 | `M` | `K` | `N` | warmup | iters |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 1024 | 1024 | 2048 | 5 | 50 |
| 2 | 2048 | 4096 | 3072 | 5 | 30 |
| 3 | 4096 | 4096 | 4096 | 5 | 20 |
| 4 | 4097 | 3073 | 2305 | 5 | 20 |
| 5 | 8192 | 2048 | 4096 | 5 | 15 |
| 6 | 1536 | 6144 | 6144 | 5 | 15 |

优化提示：

可以把本题看作两个共享 `x` 的 GEMM 加一个融合 epilogue。常见优化方向包括复用 `x` 的 tile 加载、同时计算 gate/up 两个分支、把 bias、SiLU 和逐元素乘融合到输出阶段、避免把两个中间矩阵完整写回全局内存。非整除测试点需要正确处理边界 mask。

性能统计口径中，FLOPs 约为 `4 * M * K * N + 8 * M * N`，主访存按读取 `x`、两份权重、两份偏置并写出 `y` 的外部 tensor 流量估算。
